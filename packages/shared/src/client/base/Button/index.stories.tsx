import { Button } from ".";

const meta = {
  title: "Components/Button",
};

export default meta;

export const Example = {
  render: () => {
    return <Button onClick={() => {}}>button</Button>;
  },
};
